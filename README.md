# Non-Shared-Files-Assignment

The secrets that needed to be passed were the API_KEY: "12345ABCDE" and the PASSWORD: "mySuperSecretPassword". Without these you will get the console error -> "API_KEY or PASSWORD is not defined in the environment variables.".

The "." in front a file basically tells your computer that this file should be hidden by default. It also serves as a known convention for developers to identify configuration files.